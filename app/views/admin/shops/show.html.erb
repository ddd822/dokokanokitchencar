<div class="row mt-3">
  <div class="col-3">
    <h3>店舗ユーザー詳細</h3>
    <table class="table">
      <tbody>
        <tr>
          <th>id</th>
          <td><%= @shop.id %></td>
        </tr>
        <tr>
          <th>name</th>
          <td><%= @shop.name %></td>
        </tr>
        <tr>
          <th>email</th>
          <td><%= @shop.email %></td>
        </tr>
        <tr>
          <th>電話番号</th>
          <td><%= @shop.telephone_number %></td>
        </tr>
        <tr>
          <th>店舗紹介</th>
          <td><%= @shop.introduction %></td>
        </tr>
      </tbody>
    </table>
    <%= link_to "削除", admin_shop_path(@shop), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "btn btn-outline-danger btn-block" %>
  </div>
  <div class="col-8 offset-1">
    <h3><%= @shop.name %>の投稿一覧</h3>
    <% if @posts.any? %>
      <table class="table">
        <thead>
          <tr>
            <th>id</th>
            <th>title</th>
            <th>body</th>
            <th></th>
          </tr>
        </head>
        <tbody>
          <% @posts.each do |post| %>
            <tr>
              <td><%= link_to post.id, admin_post_path(post) %></td>
              <td><%= post.title %></td>
              <td><%= post.body %></td>
              <td><%= link_to "詳細", admin_post_path(post), class: "btn btn-outline-primary" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% else %>
      <p>まだ投稿がありません。</p>
    <% end %>
  </div>
  <div class="my-2">
  <%= link_to '店舗ユーザー一覧へ戻る', admin_shops_path %>
  </div>
</div>

<div class="row">
  <div class='col-md-5 offset-md-1'>
    <h2>プロフィール</h2>
      <table class='table'>
      <tr>
        <th>ユーザーネーム</th>
        <td><%= @customer.name %></td>
      </tr>
      <tr>
	      <th>メールアドレス</th>
	      <td><%= @customer.email %></td>
      </tr>
    </table>
    <% if customer_signed_in? && @customer == current_customer %>
      <div class='col-12 my-3'>
        <%= link_to 'プロフィールを編集する', edit_customer_path(@customer), class: "btn btn-sm btn-success" %>
      </div>
      <div class='col-12 my-3'>
        <%= link_to '退会する', customer_path(@customer), method: :delete, data: { confirm: '本当に退会しますか？' }, class: "btn  btn-sm btn-danger" %>
      </div>
    <% end %>
  </div>
  <div class='col-md-5 offset-md-1'>  
    <h2>あなたの投稿一覧</h2>
    <% if @posts.any? %>
      <ul>
        <% @posts.each do |post| %>
          <li>
            <%= link_to post.title, post_path(post) %> 
            <small>(投稿日: <%= post.created_at.strftime("%Y/%m/%d") %>)</small>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p>まだ投稿がありません。</p>
    <% end %>
  </div>
</div>